# Dealing with ServerPilot

## PHP
PHP on ServerPilot is installed a little bit differently. Versions coexist and they're located at `/opt/sp/php-...`. You'll find `php-config` and `phpize` and co. binaries there.